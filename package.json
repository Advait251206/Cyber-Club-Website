{
  "name": "cyber-club-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && node server.js\"",
    "start": "cd backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
